{
  "name": "json-server-starter",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "start": "json-server -w db.json"
  },
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
